/**
 * Wishlist-module request schemas. Definitions live in `@roomadda/shared`
 * (single source of truth, see /CLAUDE.md); re-exported under local names.
 */
export { wishlistParamSchema, wishlistQuerySchema } from "@roomadda/shared";
export type { WishlistQuery } from "@roomadda/shared";
