/**
 * Users-module request schemas. Definitions live in `@roomadda/shared` (single
 * source of truth, see /CLAUDE.md); this module re-exports under local names.
 */
export { updateProfileSchema } from "@roomadda/shared";
export type { UpdateProfileInput } from "@roomadda/shared";
