# Root environment for docker-compose. Copy to `.env` and adjust for local dev.
# docker compose reads this file automatically.

# ---- PostgreSQL (PostGIS) --------------------------------------------------
POSTGRES_USER=roomadda
POSTGRES_PASSWORD=change_me_in_local_env
POSTGRES_DB=roomadda
POSTGRES_PORT=5432

# ---- Redis -----------------------------------------------------------------
REDIS_PORT=6379
